#continue is used to stop the current iteration of the loop and continue with the next one.
# It instructs the python to"skip this iteration" 

if a in range(10):
    if a == 5:
     continue
print(a)